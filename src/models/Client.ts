// Modèle MongoDB retiré. Utilisez Supabase (tables) à la place.
export default function MongoClientModelStub(): never {
  throw new Error('Client (Mongo) retiré. Utilisez la table correspondante dans Supabase.');
}